
import { SalarySimulatorComponent } from './salary-simulator/salary-simulator.component';
import { Routes } from '@angular/router';

export const routes: Routes = [
	{ path: '', component: SalarySimulatorComponent },
];
